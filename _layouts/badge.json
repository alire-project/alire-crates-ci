{
    "schemaVersion": 1,
    "label": "CI {{ page.crate }}",
    "message": "{{ page.success }}/{{page.total}}",
    "logoSvg": {{ site.data.badge.logoSvg | jsonify }},
    "color": "{{ page.color }}"
}
