{
    "schemaVersion": 1,
    "label": "CI {{ page.crate }}",
    "message": "{{ page.success }}/{{page.success | plus: page.fail}}",
    "logoSvg": {{ site.data.badge.logoSvg | jsonify }},
    "color": "{{ page.color }}"
}
